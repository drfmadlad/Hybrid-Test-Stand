[env:teensy41]
platform = teensy
board = teensy41
framework = arduino

; FlexCAN_T4 library - install from GitHub
lib_deps =
    https://github.com/tonton81/FlexCAN_T4.git
    adafruit/Adafruit BMP3XX Library

; Build flags for optimization and debugging
build_flags = 
    -D USB_SERIAL
    -D ARDUINO_TEENSY41
    -O2

; Serial monitor configuration
monitor_speed = 2000000

; Upload configuration (automatic via Teensy Loader)
upload_protocol = teensy-cli

; Only compile the unified main.cpp — individual test files are archived
build_src_filter = +<main.cpp> -<combined_test_main.cpp> -<servo_test_main.cpp> -<slf3s_flow_test_main.cpp> -<sniffer_main.cpp>


